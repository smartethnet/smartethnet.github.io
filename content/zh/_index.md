---
title: "Rustun - AI驱动的智能VPN隧道"
description: "基于 Rust 构建的高性能 VPN 隧道，具备自动路径选择和智能路由能力，用于实现设备互联，异地组网"
---

## 🌐 Rustun

**AI驱动的智能VPN隧道**

基于 Rust 构建的 AI 驱动智能 VPN 隧道，具备自动路径选择和智能路由能力。**100% 开源**，采用 MIT 许可证。

**状态：Beta** 🚧

## ✨ 核心特性

- 🔓 **开源免费** - MIT 许可证，完全免费透明，代码可审计
- ⚡ **简洁高效** - 一行命令启动：`./client -s SERVER:8080 -i client-001`
- 🏢 **多租户** - 基于集群的隔离，支持多团队或多业务单元
- 🔐 **安全加密** - ChaCha20-Poly1305（默认）、AES-256-GCM、XOR/Plain 可选
- 🚀 **双路径 P2P** - IPv6 直连 + STUN 打洞，自动降级到中继模式
- 🌐 **智能路由** - 自动选择最佳路径：IPv6（最低延迟）→ STUN（NAT穿透）→ 中继
- 🌍 **跨平台** - Linux、macOS、Windows 预编译二进制文件

## 🚀 部署方式

Rustun 提供两种部署方式，满足不同需求：

### 🏠 自托管（推荐，完全自主控制）

在自己的基础设施上部署 Rustun，获得完全的控制权和隐私保护。

**优势：**
- ✅ 完全的数据主权 - 数据完全保留在您的服务器上
- ✅ 完全自定义 - 按需配置所有功能
- ✅ 无供应商锁定 - 100% 开源
- ✅ 成本效益 - 无订阅费用
- ✅ 企业级就绪 - 适用于生产环境

**快速开始：**

```bash
# 一键服务端安装
curl -fsSL https://raw.githubusercontent.com/smartethnet/rustun/main/install.sh | sudo bash

# 配置
sudo vim /etc/rustun/server.toml
sudo vim /etc/rustun/routes.json

# 启动服务
sudo systemctl start rustun-server
sudo systemctl enable rustun-server
```

**了解更多：** [自托管设置指南](https://github.com/smartethnet/rustun#installation)

### ☁️ 官方服务（托管解决方案）

使用我们托管的 Rustun 服务，无需部署服务端，通过可视化管理页面和AI交互式管理，享受无忧部署和专业支持。

**优势：**
- ✅ 无需部署服务端 - 立即开始使用
- ✅ 可视化管理页面 - 直观的Web界面进行网络管理
- ✅ AI交互式管理 - 智能辅助配置和故障排除
- ✅ 零维护 - 我们负责服务器管理
- ✅ 专业支持 - 需要时获得专家协助
- ✅ 高可用性 - 冗余基础设施

**开始使用：** [访问官方服务](https://rustun.beyondnetwork.cn)

## 🚀 快速开始（自托管）

### 一键安装

**服务端安装：**

```bash
# 自动安装最新版本
curl -fsSL https://raw.githubusercontent.com/smartethnet/rustun/main/install.sh | sudo bash

# 配置
sudo vim /etc/rustun/server.toml
sudo vim /etc/rustun/routes.json

# 启动服务
sudo systemctl start rustun-server
sudo systemctl enable rustun-server
```

### 下载预编译二进制文件

从 [GitHub Releases](https://github.com/smartethnet/rustun/releases/latest) 下载

**支持的平台：**
- **Linux**: x86_64 (glibc/musl), ARM64 (glibc/musl)
- **macOS**: Intel (x86_64), Apple Silicon (ARM64)
- **Windows**: x86_64 (MSVC)

**每个版本包含：**
- `server` - VPN 服务端二进制文件
- `client` - VPN 客户端二进制文件
- `server.toml.example` - 服务端配置模板
- `routes.json.example` - 路由配置模板

### 连接客户端

```bash
# 基本连接
sudo ./client -s SERVER_IP:8080 -i client-001

# 使用自定义加密
./client -s SERVER:8080 -i client-001 -c chacha20:my-secret-key

# 启用 P2P 模式
./client -s SERVER:8080 -i client-001 --enable-p2p
```

## 🚀 P2P 直连

启用 P2P 模式实现点对点直连，自动智能路径选择：

```bash
./client -s SERVER:8080 -i client-001 --enable-p2p
```

### 连接策略

Rustun 使用三层智能路由策略：

1. **🌐 IPv6 直连**（首选路径）
   - 最低延迟，最高吞吐量
   - 当双方都有全局 IPv6 地址时工作
   - 自动建立连接

2. **🔄 STUN 打洞**（次选路径）
   - IPv4 网络的 NAT 穿透
   - 支持大多数 NAT 类型
   - IPv6 不可用时自动降级

3. **📡 中继模式**（兜底方案）
   - P2P 失败时通过服务器转发
   - 保证连通性
   - 自动故障转移

## 🏢 多租户隔离

Rustun 支持基于集群的多租户架构，实现不同团队或业务单元之间的完全网络隔离。

### 工作原理

- 每个客户端属于一个**集群**
- 客户端只能与同一集群内的对等节点通信
- 不同集群使用独立的 IP 地址范围
- 完美适用于隔离生产、预发布和开发环境

## 💼 使用场景

Rustun 适用于各种网络场景：

- **🏢 远程办公连接** - 连接多个办公地点，实现站点到站点 VPN
- **👨‍💻 安全远程办公** - 为在家办公的员工提供安全的企业内网访问
- **🔀 多环境隔离** - 为生产、预发布和开发环境分离网络
- **🤖 IoT 设备管理** - 安全地连接和管理跨地域的 IoT 设备
- **🎮 游戏服务器网络** - 跨区域的低延迟游戏服务器网络
- **☁️ 混合云连接** - 连接本地基础设施和云资源
- **🔐 零信任网络** - 构建具有对等节点隔离的零信任网络

## 🔓 为什么选择开源？

Rustun **100% 开源**，因为我们相信：

- **透明性** - 代码可审计，无隐藏后门
- **安全性** - 社区审查使软件更安全
- **自由** - 无供应商锁定，您拥有自己的基础设施
- **创新** - 社区贡献推动持续改进
- **信任** - 开放开发建立用户信心

**查看源代码：** [GitHub 仓库](https://github.com/smartethnet/rustun)

## 📚 文档

详细文档请访问：
- [GitHub README](https://github.com/smartethnet/rustun#readme)
- [协议文档](https://github.com/smartethnet/rustun/blob/main/PROTOCOL.md)
- [构建指南](https://github.com/smartethnet/rustun/blob/main/BUILD.md)
- [贡献指南](https://github.com/smartethnet/rustun/blob/main/CONTRIBUTING.md)

## 🤝 贡献

我们欢迎贡献！详情请参阅[贡献指南](https://github.com/smartethnet/rustun/blob/main/CONTRIBUTING.md)。

## 📞 联系方式

- 问题反馈: [GitHub Issues](https://github.com/smartethnet/rustun/issues)
- 讨论区: [GitHub Discussions](https://github.com/smartethnet/rustun/discussions)
- 官方服务: [访问官方服务](https://rustun.beyondnetwork.cn)

---

**注意**：这是一个实验性项目。在生产环境中使用需自行承担风险。
